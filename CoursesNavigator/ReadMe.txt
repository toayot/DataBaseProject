นำไฟล์ทั้งหมดใส่ใน xampp\htdocs\
import cscoursenavigator(Structure).sql
import cscoursenavigator(Data).sql

แล้วเข้าลิงค์เพื่อเริ่มการทำงาน
http://localhost/cscoursenavigator/loginpage.html

user ทดลองที่ใช้ในการพรีเซ้น
ID : 6109610052
Password : 999999999